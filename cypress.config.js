module.exports = {
  e2e: {
    // Configurações do Cypress (exemplo)
    baseUrl: 'http://localhost:3000',
    viewportWidth: 1280,
    viewportHeight: 720,
    setupNodeEvents(on, config) {
      // Você pode adicionar eventos aqui, como plugins
    },
  },
};
